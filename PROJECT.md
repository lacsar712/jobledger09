# JobLedger · 电视播出 as-run 对账

## 这不是什么

不是 OA、不是考勤、不是「任务看板」、不是招聘 Job Board。记录是**一条播出事件**：节目单时刻 vs 播控 GPI 实际落下的时刻。

## 谁在用

地面频道播控机房。每天节目单（playlist）下发到切换台；实际播出以 GPI/时钟脉冲为准。广告插播、延时、应急垫片会导致「单上写的」和「真正送出去的」差几帧。广告客户按秒结算，差值超过窗口就要打 discrepancy。

## 核心业务

1. 正文必须带 `playlist_ms=` 与 `aired_ms=`（毫秒，相对节目单零点）。
2. 默认容差 150ms（约 4 帧 @25fps）。超出且未打标签 `manual-ok` 的条目拒绝入库。
3. 标签描述事件类型：`prog` / `spot` / `ident` / `filler`。
4. 对账流水线：ingest → match → discrepancy 列表，而不是空跑。
5. 修订链保存每次人工放行（`manual-ok`）的操作者。
6. 附件是切换台日志切片，同样禁止路径逃逸。

## 运行与验收

- 控制台可录入 as-run 条、按片名检索。
- 故意把 `|playlist-aired|` 拉到 400ms 且无 `manual-ok`，创建必须失败。
- 不提供排班、请假、审批流。
